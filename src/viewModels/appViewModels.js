export function createIntroView(baseView, extras = {}) {
  return { ...baseView, ...extras };
}

export function createResultView(baseView, extras = {}) {
  return { ...baseView, ...extras };
}

export function createPlayView(baseView, extras = {}) {
  return { ...baseView, ...extras };
}
