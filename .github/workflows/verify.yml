name: Verify

on:
  pull_request:
    types: [opened, synchronize, reopened, labeled, unlabeled]
  push:
    branches:
      - main
      - master

jobs:
  verify-quick:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
      # Node comes from .node-version, which is also what the Render build
      # reads. CI used to say `node-version: 24` while that file said 22.16.0,
      # so every check ran on a major the deploy never built with.
      - uses: actions/setup-node@v5
        with:
          node-version-file: .node-version
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - run: npm run verify:quick
      - name: Summarize bundle and test budgets
        if: always()
        run: |
          echo '### Verification summary' >> "$GITHUB_STEP_SUMMARY"
          npm run check:bundle >> "$GITHUB_STEP_SUMMARY" 2>&1 || true
          npm run check:export-schema >> "$GITHUB_STEP_SUMMARY" 2>&1 || true
      - name: Upload Playwright diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: quick-playwright-diagnostics
          path: |
            test-results
            playwright-report
          if-no-files-found: ignore
          retention-days: 7

  verify-e2e:
    if: >-
      github.event_name != 'pull_request' ||
      contains(github.event.pull_request.labels.*.name, 'e2e')
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v5
      # One Node version for CI and the Render build: .node-version.
      - uses: actions/setup-node@v5
        with:
          node-version-file: .node-version
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - run: npm run verify:e2e
      - name: Summarize E2E result
        if: always()
        run: echo 'E2E suite completed; see the Playwright artifact for detailed results.' >> "$GITHUB_STEP_SUMMARY"
      - name: Upload E2E diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-playwright-diagnostics
          path: |
            test-results
            playwright-report
          if-no-files-found: ignore
          retention-days: 7
