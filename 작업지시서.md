# 임계점(Critical Point) — Codex 작업지시서 v1.0

- 작성일: 2026-08-28
- 기준 커밋: `ea8778c Improve-burst-scoring-and-branch-ux`
- 대상 실행자: Codex
- 목적: (1) "분기 중 특정 지점에서 이야기가 끊기는" 문제의 실제 원인 제거, (2) 에러를 실제로 다 잡을 수 있는 관측 체계 구축, (3) 그 위에서 UI/UX·게임성·엔딩을 재설계

---

## 30초 요약

1. **스토리 그래프에는 버그가 없습니다.** 87노드·306선택지를 정적 분석과 화면 전수 크롤(7.7분)로 모두 확인했고 실패 0건입니다.
2. **버그는 저장 계층에 있습니다.** `persist()`가 `discoveredClues` 키 하나를 빠뜨려서, 평범한 저장(자유입력 타이핑, 탭 전환, `P` 저장) 뒤 새로고침하면 앱이 세이브를 "손상됨"으로 판정합니다. 그러면 단서가 0으로 지워지고, 진행이 강제로 일시정지되며, **존재하지 않는 에러가 만들어져** 플레이어에게 복구 배너가 뜹니다. → **D-1**
3. **에러 로그를 모아도 못 잡은 이유가 둘 있습니다.** (a) 루트의 `debug.log`는 게임과 무관한 Chromium 로그입니다(215줄 전부). (b) 진행이 끊기는 지점 대부분이 예외가 아니라 **조용한 폴백**이라 애초에 기록되지 않고, 20칸짜리 로그는 위 가짜 에러로 계속 덮입니다. → **D-2**
4. **이 게임에는 실제 분기가 없습니다.** 87개 노드 전부 다음 장면이 하나뿐입니다. "분기"는 케이스 시작 도입문 3종뿐이고 두 번째 장면에서 합류합니다. 장면 87개 중 51개(59%)가 템플릿 자동 생성 또는 복제이고, 선택지 306개 중 147개가 동일한 기본 에코 문구로 폴백합니다. → **PHASE 4의 출발점**
5. **엔딩 대사가 정규식에 깨져 있습니다.** `simplifyPlayerText`가 완성 원고를 런타임에 치환해 "자신의 **반응 버튼를** 약점으로만"처럼 조사가 파괴됩니다(12곳, 엔딩 포함). → **D-3**
6. **그리고 "새 게임으로 시작" 버튼이 아무 일도 하지 않습니다.** 저장본을 지운 직후 `reload()`가 `pagehide`를 발생시키고, 그 핸들러가 저장본을 다시 씁니다. 5번의 가짜 배너와 합쳐지면 **배너가 뜨는데 어떤 버튼으로도 벗어날 수 없는 상태**가 됩니다. → **D-13**

**가장 먼저 할 일: T1-1 + T1-1B(각각 몇 줄) → T1-3(누적 상태 완주 크롤러).** 이 셋이면 "그 지점"의 정답지가 나옵니다.

---

## 0. 이 문서를 Codex에게 쓰는 방법

이 저장소는 "기능을 계속 덧붙이는" 방식으로 137커밋이 쌓였습니다. 그 결과 한 파일에 5,410줄(`src/main.jsx`), 스타일 6,713줄(`src/styles.css`)이 몰려 있고, 같은 상수가 4곳에 중복되어 있습니다. 이 상태에서 "개선해줘"라고 넓게 요청하면 **또 하나의 레이어가 추가되고 근본 원인은 남습니다.** 실제로 지금 코드에는 저장 데이터 정규화 함수가 12개, 복구 슬롯, 에러 바운더리, 원격 에러 텔레메트리까지 있는데도 **진짜 버그는 한 번도 에러로 기록되지 않습니다.**

그래서 이 문서는 다음 규칙을 전제로 합니다.

1. **한 번에 TASK 하나만** 넣습니다. TASK 전문을 그대로 붙여넣으십시오.
2. 모든 TASK는 `[증거] → [원인] → [지시] → [금지] → [검증]` 5블록입니다. **[검증]에 적힌 명령이 통과해야 그 TASK는 끝난 것입니다.**
3. `[금지]` 블록을 절대 삭제하지 말고 함께 넣으십시오. Codex의 기본 성향(새 방어 레이어 추가, 파일 확장, 상수 재선언, "관련 개선" 끼워넣기)을 차단하는 장치입니다.
4. PHASE 1을 전부 끝내기 전에는 PHASE 3 이후로 넘어가지 마십시오. 지금의 UI 문제 중 상당수는 PHASE 1 버그의 증상입니다.
5. 커밋은 TASK 1개 = 커밋 1개. 커밋 메시지에 TASK 번호를 넣으십시오. (예: `T1-1 persist가 discoveredClues를 저장하도록 수정`)

---

## 1. 코드베이스 실측치

| 항목 | 수치 | 메모 |
|---|---|---|
| `src/main.jsx` | 5,410줄 | `App()` 단일 컴포넌트가 952~5,380줄. 플레이 화면 한 장에 패널 블록 약 30개 |
| `src/styles.css` | 6,713줄 | 클래스 셀렉터 1,149개, `:root` 블록 2개(1줄·5,445줄), `var(--)` 사용 47회 vs 하드코딩 hex 127종·`rgba()` 145회, 미디어쿼리 6개 |
| `src/gameData.js` | 1,836줄 | 노드 87개 |
| `src/gameLogic.js` | 849줄 | 순수 함수 계층. 상태적으로 가장 건강한 파일 |
| 폰트 크기 선언 | 10~13px가 189개 / 26px 이상은 3개 | 타입 스케일 부재 |
| `scripts/smoke-test.mjs` | assert 153개 | 순수 함수·그래프에 집중 |
| `tests/season-flow.spec.js` | e2e 32개 | 대부분 저장/복구/텔레메트리. 스토리 진행은 사실상 1경로 |

### 노드 구성 (87개)

| 종류 | 개수 | 저작 방식 |
|---|---|---|
| 손으로 쓴 본편 장면 | 30 | 개별 집필 |
| 후폭풍(aftershock) | 6 | 개별 집필 |
| 연결 장면(connective) | 18 | **템플릿 자동 생성** |
| 반응 장면(reaction) | 18 | **템플릿 자동 생성** |
| 분기 브리핑(branch briefing) | 15 | 본편 장면 **복제** + 도입문만 교체 |

**87개 중 51개(59%)가 자동 생성 또는 복제입니다.** 자동 생성된 36개 장면의 선택지 효과는 단 3종 템플릿이 그대로 반복됩니다.

```
index 0 -> { trust: 4,  legitimacy: 3, fatigue: 5 }
index 1 -> { time: -4,  legitimacy: 5, fatigue: 4 }
index 2 -> { capital: 5, trust: -5,    fatigue: 2 }
```

(`gameData.js:1685` `addConnectiveScene`, `gameData.js:1755` `addReactionScene`)

또한 306개 선택지 중 **147개가 전용 대사(`choiceVoiceLines`)와 전용 에코 응답(`echoReplies`)이 없어 라벨/기본 문구로 폴백**합니다. 즉 절반 가까운 선택에서 에코가 똑같은 문장("그 판단을 유지하려면 숨은 피해자와 비용을 다시 계산해야 합니다…")을 반복합니다.

→ 이것이 "장면은 많은데 이야기가 이어지는 느낌이 없다"의 구조적 원인입니다.

---

## 2. 진단 — "분기 에러"의 실체

### 2.1 스토리 그래프 자체는 이미 정상입니다

정적 검증을 직접 실행한 결과:

```
dangling choice.next  : 0건
노드 87개             : 전부 도달 가능, 고아 노드 0
케이스별 순서/도달성  : case01~final 전부 order 길이 == 도달 노드 수
케이스 간 누수        : 0건
(node, choice) 쌍     : 306개, 전부 유효한 next 보유
```

`scripts/smoke-test.mjs`는 이미 306쌍 전수 + 랜덤 시드 200회 완주를 검증합니다(360~480줄). **Codex는 "그래프가 안전한지"를 이미 과잉 검증하고 있고, 버그는 거기에 없습니다.**

### 2.2 그리고 이 게임에는 사실상 분기가 없습니다

87개 노드 **전부** `choice.next`가 단 하나입니다. 어떤 선택을 해도 다음 장면은 같습니다.

- 진짜 갈림길은 케이스 시작 시점 3갈래(`c2_start_people` / `c2_start_records` / `c2_start_silence`)뿐이고,
- 그 3갈래도 선택지 배열이 원본 노드의 **복제**이며 두 번째 장면(`c2_logs`)에서 즉시 합류합니다.

> **핵심 인식 차이:** "분기되는 모든 결과의 경로가 존재한다"고 인식하고 계시지만, 실제로는 **도입 문단만 다르고 경로는 하나**입니다. 이 사실이 PHASE 4 설계의 출발점입니다.

### 2.3 따라서 에러는 데이터가 아니라 React/스토리지 계층에 있습니다

그리고 실제로 거기에 있습니다. → 3장.

### 2.4 `debug.log`는 앱 로그가 아닙니다

루트의 `debug.log`(38KB, 215줄)를 전수 확인한 결과:

```
147줄  ERROR:gpu\command_buffer\service\shared_image\shared_image_manager.cc
 66줄  WARNING:services\audio\sync_reader.cc  (ASR: No room in socket buffer)
  2줄  WARNING:services\audio\sync_reader.cc:175 SyncReader::Read timed out
```

**앱 관련 항목 0줄.** Chromium/Electron의 GPU·오디오 로그이며 `.gitignore`에도 들어 있습니다. **지금까지 모은 "에러 로그"는 게임과 무관합니다.** 실제 에러는 브라우저 `localStorage`의 `trigger-prototype-error-log-v1`(최대 20건)에만 있고, 그 20칸은 D-2에서 설명하는 **가짜 에러로 계속 덮이고 있습니다.**

---

## 3. 확정 결함 목록 (전부 재현 완료)

### D-1 【치명】 `persist()`가 `discoveredClues`를 누락 → 매 저장마다 세이브가 구조적으로 무효

**증거 — 실제 브라우저 재현 로그 (Playwright, chromium)**

```
[1] 단서 1개 보유          clues: 1   paused: false  lastError: null
[2] 평범한 저장(pagehide) 후 → has discoveredClues key: false   clues: MISSING
[3] 새로고침 후            clues: 0   paused: true
                           lastError.source : save-integrity
                           lastError.message: "Saved nested gameplay data was repaired before resume."
[4] 플레이어에게 복구 알림 노출: true
[5] 복구 슬롯 자동 생성   : 2개
[6] 다시 새로고침          paused: true   lastError.source: save-integrity   ← 계속 반복
```

**원인**

`src/main.jsx:1838` `persist()`의 기본 payload에 `discoveredClues`가 없습니다.

```
saveSchemaVersion, playerName, playStyle, openingLegacy, dataConsent, started,
currentCase, completedCases, caseResults, playtestFeedback, nodeId, resources, log,
triggers, cognition, freeText, echo, nodeEnteredAt, pendingTelemetry, protocolUsed,
timerPenaltyApplied, probeUsed, paused, savedAt
                                       ↑ discoveredClues 없음
```

`discoveredClues`를 명시적으로 넘기는 곳은 `choose()`(2628줄)와 `startGame()`(1917줄) **단 2곳**입니다. 나머지 저장 경로는 전부 이 키를 지웁니다.

| 위치 | 트리거 | 빈도 |
|---|---|---|
| `main.jsx:2161` `persist({freeText})` | 자유입력 **키 입력 한 글자마다** | 매우 잦음 |
| `main.jsx:1764` `persist({paused:true})` | pagehide(탭 닫기/이동) | 매번 |
| `main.jsx:1755` `persist({nodeEnteredAt})` | 탭 복귀 | 매번 |
| `main.jsx:1811` 타임아웃 패널티 | 45초 초과 | 잦음 |
| `main.jsx:1969` `saveCurrentGame()` | `P` / `Shift+P` 저장 | 사용자가 의도적으로 |
| `main.jsx:2948/2970` 피드백 저장 | 결과 화면 | 매번 |
| `main.jsx:3617` 플레이 스타일 변경 | 인트로 | 매번 |

그리고 `src/appConfig.js:76` `isSavedStateShapeValid()`는 `discoveredClues`가 배열일 것을 **요구**합니다. 로드 시(`main.jsx:953`) `normalizeSavedNestedState()`가 이를 "손상된 저장본"으로 판정하고 자가복구하면서 `main.jsx:783-791`에서 **존재하지 않는 에러를 만들어 냅니다.**

**영향**
- 발견한 숨은 단서(EVIDENCE)가 매번 0으로 초기화 → 엔딩의 `{clueCount}/6`은 사실상 항상 0/6
- 저장본이 강제로 `paused: true` → 정상 플레이인데 "복구 알림"이 뜸
- 가짜 `save-integrity` 에러가 계속 생성 → **D-2로 이어짐**
- 새로고침 때마다 복구 슬롯(최대 5칸)이 소모됨

**플레이어 입장에서 보이는 모습 — 이것이 "이야기가 안 이어진다"의 실체입니다**

새로고침 직후 이야기 위에 이런 배너가 뜹니다(`main.jsx:3235` `renderRecoveryNotice`).

```
복구됨
case02 / c2_trace
Saved nested gameplay data was repaired before resume.
같은 지점에서 오류가 반복되면 다시 시도하지 말고, 저장 지점을 일시정지한 뒤
새 게임 또는 복구 슬롯을 선택하세요.

[저장 지점 일시정지]  [새 게임으로 시작]  [에러 로그]  [닫기]
```

에러 배너를 본 플레이어의 자연스러운 반응은 **"새 게임으로 시작"** 입니다. 그런데 그 버튼은 **아무 일도 하지 않습니다**(→ D-13). 같은 장면, 같은 배너가 그대로 남습니다. "저장 지점 일시정지"를 눌러도 다음 새로고침에 배너가 다시 뜹니다.

버그가 없는데 버그 안내가 뜨고, 그 안내를 따라도 벗어날 수 없습니다. 이 한 줄(`persist()`의 키 누락)이 시작점이고, D-13이 출구를 막습니다.

---

### D-2 【치명】 가짜 에러가 진짜 에러를 밀어냅니다 — "에러 로그를 모아도 못 잡는" 직접 원인

`ERROR_LOG_MAX_ITEMS = 20`(`appConfig.js:3`), `SAVE_SLOT_MAX_ITEMS = 5`(`appConfig.js:6`)입니다. D-1이 만든 `save-integrity` 항목과 반복 생성되는 복구 슬롯이 이 칸을 계속 채웁니다.

동시에, **실제로 진행을 끊는 실패 대부분은 애초에 에러로 기록되지 않습니다.**

| 실패 유형 | 현재 처리 | 기록되는가 |
|---|---|---|
| 저장본 형태 불일치 | `main.jsx:956` `return null` | ❌ 조용히 새 게임 |
| `nodeId`가 케이스와 불일치 | `main.jsx:551` `repairSavedRoute`가 시작 장면으로 되돌림 | 알림만, 원인 없음 |
| `nodes[nodeId]` 미존재 | `main.jsx:1050` `?? fallbackNodeId`, `main.jsx:1066` `?? nodes.start` | ❌ 조용히 다른 장면 |
| `choice.next` 미존재 | `choose()`에 검증 없음 (`main.jsx:2500`) | ❌ 빈 화면 |
| 저장 로그 엔트리 정규화 실패 | `main.jsx:661` `return null` (엔트리 삭제) | ❌ 조용히 삭제 |
| React 렌더 예외 | 에러 바운더리 | ✅ |
| `console.error` / 리액트 경고 | 미수집 | ❌ |

**"이야기가 안 이어진다"의 대부분은 예외가 아니라 조용한 폴백입니다.** 예외가 나지 않으니 에러 로그에 남을 리가 없습니다. 이것이 문제의 본질입니다.

---

### D-3 【높음】 `simplifyPlayerText`가 완성 원고를 정규식으로 덮어써 문장을 파괴합니다

`src/playerLanguage.js`의 67개 치환 규칙이 **작가가 쓴 본문 그대로**에 적용됩니다(`main.jsx:4754` 본문, `4913` 선택지, `4527` 제목, `3126` 장면 비트, 총 62곳).

실제 출력 (직접 확인):

| 노드 | 원문 | 화면 출력 |
|---|---|---|
| `f_choice` (**엔딩 장면**) | 당신은 자신의 **트리거를** 약점으로만 볼 수도 있고 | 당신은 자신의 **반응 버튼를** 약점으로만 볼 수도 있고 |
| `f_archive` 선택지 | **트리거** 프로필 데이터 폐기를 요구한다 | **반응 버튼** 프로필 데이터 폐기를 요구한다 |
| `c2_logs` 본문 | **로그는** 지나치게 깔끔합니다 | **기록는** 지나치게 깔끔합니다 |
| `c2_logs` 선택지 | 로그 원본과 백업 **로그를** 대조한다 | 기록 원본과 백업 **기록를** 대조한다 |
| `f_start` 본문 | 모든 **케이스가** 끝난 뒤, **케이스데스크에** | 모든 **사건가** 끝난 뒤, **사건데스크에** |
| `competitor` 본문 | 오진우 **분석관은** | 오진우 **담당자은** |
| `c3_trap` 본문 | 이 **케이스는** 입찰 문제가 아닐 수 있습니다 | 이 **사건는** 입찰 문제가 아닐 수 있습니다 |

- 본문이 재작성되는 노드: **10/87**
- 선택지 라벨이 재작성되는 건수: **14건**
- 조사(은/는/이/가/을/를)가 깨진 문자열: **12건** — 여기에 **엔딩 대사와 최종 선택지가 포함**됩니다.

시즌 클라이맥스가 "자신의 반응 버튼를"로 출력되는 상태에서 "엔딩의 여운"을 논할 수 없습니다.

---

### D-4 【높음】 진행률이 분기마다 다른 값에서 시작합니다

`src/gameData.js:1835`

```js
nodeOrders[caseId].unshift(...Object.values(routes));  // 분기 브리핑 3개를 순서 배열 맨 앞에 삽입
```

`nodeOrders.case02` = `[c2_start_people, c2_start_records, c2_start_silence, c2_start, c2_logs, ...]` (15개)인데, **실제 플레이 경로는 12개**입니다(브리핑 1개만 방문, `c2_start`는 디버그로만 도달).

`src/main.jsx:3019`

```js
const progress = Math.round(((activeNodeOrder.indexOf(resolvedNodeId) + 1) / activeNodeOrder.length) * 100);
```

| 진입 분기 | 첫 화면 진행률 |
|---|---|
| `c2_start_people` | **7%** |
| `c2_start_records` | 13% |
| `c2_start_silence` | **20%** |

같은 위치인데 분기에 따라 진행률이 다르고, 중간에 3칸이 통째로 건너뛰어집니다. case02~final 전부 해당됩니다(case01만 정상).

---

### D-5 【중】 엔딩 성향 판정에 연산자 우선순위 버그

`src/gameLogic.js:508`

```js
const stabilizerScore = ledger.riskDrops * 3 - ledger.riskRises + (ledger.totals.fatigue ?? 0) < 0 ? 6 : ledger.riskDrops * 3 - ledger.riskRises;
```

`+`가 `<`보다 우선하므로 실제 조건은 `(riskDrops*3 - riskRises + fatigue) < 0` 입니다. 의도는 `(ledger.totals.fatigue ?? 0) < 0`으로 보입니다. 이 값이 `GUARDIAN / RE-FRAMER / PRESSURE PILOT` 최종 판정(509~513줄)을 결정하므로 **마지막에 보여주는 "당신은 어떤 사람인가" 결과가 잘못 나옵니다.**

---

### D-6 【중】 CRISIS PROTOCOL이 점수를 부풀립니다

`src/main.jsx:2214` CRISIS PROTOCOL 엔트리는 `challenge: { matched: true }`를 넣으면서 `isSystemEvent`를 넣지 않습니다(ECHO PROBE `2191`, TIMEOUT `1802`는 넣음). 결과적으로

- `challengeClearCount` +1
- `currentChallengeStreak` 유지/증가 (PERFECT RUN 조건에 무료 기여)
- `rhythmScore` · `cognitionScore` 표본에 포함

→ 위험 압력 60 이상에서 프로토콜만 눌러도 연속 목표 보상이 공짜로 들어옵니다.

---

### D-7 【중】 케이스 요약이 한 수 밀린 자원으로 계산됩니다

`src/main.jsx:2248` `buildCaseSummary()`가 클로저의 `resources`(직전 값)를 그대로 넘깁니다. `choose()`는 새 자원을 적용하기 전에 이 함수를 호출하므로, 최종 리포트의 위험 압력 기준선이 **마지막 선택 이전 값**입니다.

---

### D-8 【중】 같은 상수가 4곳에 흩어져 있습니다

| 대상 | 위치 |
|---|---|
| 케이스별 시작 노드 | `main.jsx:521`(모듈), `main.jsx:1051`(App 내부에서 **재선언·섀도잉**), `main.jsx:2066` `startCase()`의 if/else 사다리 |
| 결과 노드 ID | `main.jsx:529`(맵), `main.jsx:1065-1072`(OR 체인), `main.jsx:2502-2509`(OR 체인), `smoke-test.mjs:362`(Set) |

노드 ID를 바꾸거나 케이스를 추가할 때 4곳 중 하나를 놓칠 확률이 매우 높습니다. **"분기 어딘가에서 끊긴다"류 버그의 구조적 온상입니다.**

---

### D-9 【중】 자유입력 한 글자마다 localStorage 전체 세이브를 씁니다

`main.jsx:2158` `updateFreeText()` → `persist({ freeText })`. 600자를 입력하면 최대 600회, 매번 전체 상태를 `JSON.stringify` 하여 씁니다. 저사양 기기에서 입력 랙의 원인이며 D-1을 가장 자주 발동시키는 경로입니다.

---

### D-10 【낮음】 `renderSceneLines`의 React key 충돌 위험

`main.jsx:3126` `key={line}`. 장면 비트에 동일한 줄이 두 번 나오면 중복 키 경고가 발생합니다.

---

### D-11 【높음·검증 체계】 e2e가 항상 첫 번째 선택지만 누릅니다

`tests/season-flow.spec.js:5` `chooseFirstFixedChoice()`는 `.choices .choice`의 **first**를 클릭합니다. `completeCurrentCase()`도 이것만 반복합니다. 시즌 완주 테스트는 **306쌍 중 87쌍(모든 노드의 0번 선택지)만** 밟습니다. 나머지 219쌍은 6개 샘플 단발 테스트가 전부입니다.

**Codex가 지금까지 에러를 못 잡은 이유가 여기 있습니다.** 데이터 그래프는 306쌍 전수 검증하면서, 실제 화면은 1경로만 봅니다.

---

### D-13 【치명】 "새 게임으로 시작" 버튼이 아무 일도 하지 않습니다 — 저장본이 되살아납니다

**증거 — 실제 브라우저 재현 로그**

```
테스트 A: 저장본을 지우고 페이지를 이동
  [A1] localStorage.clear() 직후 save: 없음(정상)
  [A2] 페이지 이동 후 save: 부활함 -> started:true case:case02 node:c2_logs
  [A3] 새 페이지 화면 -> intro:false  game:true      ← 인트로로 안 감. 같은 장면으로 복귀

테스트 B: 복구 알림의 [새 게임으로 시작] 버튼 클릭
  [B1] 버튼 보임: true
  [B2] 클릭 후 save: 남아있음 -> started:true case:case02 node:c2_logs
  [B3] 화면 -> intro:false  game:true               ← 새 게임이 시작되지 않음
```

**원인**

`src/main.jsx:1762-1766`

```js
const handlePageHide = () => {
  if (!started) return;
  persist({ paused: true });      // ← React 상태 전체를 localStorage 에 다시 씀
};
window.addEventListener("pagehide", handlePageHide);
```

`startFreshAfterRecovery()`(`main.jsx:1955`)는 이렇게 동작합니다.

```js
const removed = removeStoredValue(STORAGE_KEY);   // 1. 저장본 삭제
...
window.location.reload();                          // 2. 리로드 → pagehide 발생
                                                   // 3. handlePageHide 가 persist() 로 저장본을 다시 씀
                                                   // 4. 새 페이지가 되살아난 저장본을 읽음
```

`started`가 아직 `true`이므로 가드에 걸리지 않고, **삭제한 저장본이 리로드 직전에 복원됩니다.**

**영향 — D-1과 합쳐지면 탈출 불가능한 고리가 됩니다**

1. D-1 때문에 새로고침할 때마다 가짜 "복구됨" 배너가 뜬다
2. 플레이어가 배너의 **[새 게임으로 시작]** 을 누른다
3. D-13 때문에 아무 일도 일어나지 않는다 — 같은 장면, 같은 배너
4. 다시 눌러도 같다

**"어떤 지점에서 계속 에러가 발생해 이야기가 이어지지 않는다"의 체감이 정확히 이 모습입니다.** 특정 장면의 문제가 아니라, 그 시점부터 어떤 버튼도 상태를 바꾸지 못하는 상태에 갇히는 것입니다.

> 참고: `reset()`(`main.jsx:2641`, "다시 플레이")은 `reload()`를 하지 않고 React 상태만 바꾸므로 이 버그에 걸리지 않습니다. 기존 e2e(`reset clears progress...`)가 통과해 온 이유입니다. 에러 바운더리의 `error-start-fresh`도 App이 언마운트된 뒤라 리스너가 제거되어 안전합니다. **오직 `startFreshAfterRecovery` 경로만 깨져 있고, 그 경로가 플레이어가 실제로 마주치는 경로입니다.**

---

### D-12 【진단 결과】 에러는 "한 번의 선택"이 아니라 "누적된 상태"에서 납니다

이 문서를 쓰면서 **306쌍 전수 화면 크롤을 실제로 실행**했습니다(디버그 점프 → 해당 선택지 클릭 → 확정 → 다음 화면 진입, 매번 저장소 초기화).

```
===== CRAWL DONE: 306 pairs, 0 failures =====   (소요 7.7분)
```

**단발 전이는 306쌍 전부 무결합니다.** 즉 "특정 선택지를 누르면 터진다"는 형태의 버그는 없습니다.

그렇다면 남는 가능성은 셋뿐입니다.

1. **누적 상태 의존** — `log`가 길어지고 `resources`가 극단으로 가고 `caseResults`가 쌓인 뒤에만 발생
2. **케이스 전환 경로** — 디버그 점프는 `startCase()`의 이월·분기 라우팅(`main.jsx:2090`)을 **건너뜁니다.** 실제 "다음 사건 시작" 버튼 경로는 거의 테스트되지 않았습니다
3. **저장/복원 왕복** — D-1이 여기 해당합니다

**따라서 T1-3의 크롤은 "단발 전수"가 아니라 "누적 상태 완주"여야 합니다.** 아래 T1-3 지시를 그렇게 수정했습니다.

---

# PHASE 1 — 진행 차단 제거 (최우선, 순서대로)

## T1-1. `persist()` 페이로드 누락 근절

**[증거]** D-1. 재현 로그 위 참조.

**[원인]** `src/main.jsx:1838-1864` `persist()`의 기본 payload에 `discoveredClues`가 없고, `appConfig.js:76` `isSavedStateShapeValid()`는 그것을 요구함.

**[지시]**
1. 저장 스키마의 단일 정의를 `src/appConfig.js`에 만든다.
   ```js
   // appConfig.js
   export const SAVE_STATE_KEYS = [
     "saveSchemaVersion","playerName","playStyle","openingLegacy","dataConsent","started",
     "currentCase","completedCases","discoveredClues","caseResults","playtestFeedback",
     "nodeId","resources","log","triggers","cognition","freeText","echo","nodeEnteredAt",
     "pendingTelemetry","protocolUsed","timerPenaltyApplied","probeUsed","paused","savedAt",
   ];
   ```
2. `persist()`가 이 목록을 기준으로 payload를 구성하도록 바꾼다. 목록에 있는 키가 payload에 없으면 **개발 모드에서 즉시 throw** 한다(조용한 폴백 금지).
3. `isSavedStateShapeValid()`가 요구하는 키 집합과 `SAVE_STATE_KEYS`가 어긋나면 `smoke-test`가 실패하도록 assert를 추가한다.

**[금지]**
- `isSavedStateShapeValid()`에서 `discoveredClues` 검사를 빼는 방식으로 "해결"하지 말 것.
- `normalizeSavedNestedState()`에 보정 로직을 더 추가하지 말 것. 이 TASK의 목적은 **보정이 필요 없게 만드는 것**이다.
- 새 파일·새 유틸리티 모듈을 만들지 말 것. `appConfig.js`와 `main.jsx`만 수정한다.

**[검증]**
```bash
npm test
```
그리고 아래 e2e를 `tests/save-integrity.spec.js`로 추가하고 통과시킬 것:
```js
test("평범한 저장 후 새로고침해도 단서가 유지되고 paused/lastError가 생기지 않는다", async ({ page }) => {
  await page.goto("/?debug=1");
  await page.getByTestId("debug-case-select").selectOption("case01");
  await page.getByTestId("debug-node-select").selectOption("start");
  await page.getByTestId("debug-start-node").click();
  await page.evaluate(() => {
    const s = JSON.parse(localStorage.getItem("trigger-prototype-v2"));
    s.discoveredClues = [{ id: "c1-hidden-ledger", title: "숨은 급여표", text: "x" }];
    localStorage.setItem("trigger-prototype-v2", JSON.stringify(s));
  });
  await page.evaluate(() => window.dispatchEvent(new PageTransitionEvent("pagehide", { persisted: false })));
  await page.reload();
  const s = await page.evaluate(() => JSON.parse(localStorage.getItem("trigger-prototype-v2")));
  expect(s.discoveredClues).toHaveLength(1);
  expect(s.paused).toBe(false);
  expect(s.lastError).toBeFalsy();
});
```

---

## T1-1B. 저장본 부활 고리를 끊는다 (D-13) — T1-1 직후 필수

**[증거]** D-13. 재현 로그 위 참조.

**[원인]** `main.jsx:1762` `pagehide` 핸들러가 `started`만 보고 무조건 `persist()`한다. `startFreshAfterRecovery()`가 저장본을 지운 뒤 `reload()`하면 그 사이에 저장본이 복원된다.

**[지시]**
1. 모듈 스코프에 "저장 금지" 플래그를 둔다.
   ```js
   let saveSuppressed = false;
   export function suppressSaves() { saveSuppressed = true; }
   ```
   (별도 파일을 만들지 말고 `main.jsx` 상단, `persist` 정의 근처에 둔다.)
2. `persist()` 맨 앞에서 `if (saveSuppressed) return { storageSaved: false };`
3. `startFreshAfterRecovery()`에서 **저장본을 지우기 전에** `suppressSaves()`를 호출한다. `reset()`, 에러 바운더리의 `reload({clearSave:true})`에도 동일하게 적용한다.
4. `pagehide` 핸들러의 가드를 `if (!started || saveSuppressed) return;` 로 바꾼다.

**[금지]**
- `setTimeout`으로 리로드를 미루는 방식으로 회피하지 말 것. 경쟁 상태가 남는다.
- `removeEventListener`를 수동으로 호출해 해결하려 하지 말 것. `visibilitychange`·`beforeunload`·향후 추가될 저장 경로 전부를 막아야 한다. 플래그가 정답이다.
- `persist()`의 호출부를 하나씩 고치지 말 것. **`persist()` 안에서 한 번에 막는다.**

**[검증]** 아래 두 테스트를 `tests/save-integrity.spec.js`에 추가하고 통과시킬 것.

```js
test("저장본을 지우고 페이지를 이동해도 되살아나지 않는다", async ({ page }) => {
  await page.goto("/?debug=1");
  await page.getByTestId("debug-case-select").selectOption("case02");
  await page.getByTestId("debug-node-select").selectOption("c2_logs");
  await page.getByTestId("debug-start-node").click();
  await page.waitForSelector(".game-shell");
  await page.evaluate(() => localStorage.clear());
  await page.goto("/?debug=1");
  expect(await page.evaluate(() => localStorage.getItem("trigger-prototype-v2"))).toBeNull();
  await expect(page.locator(".intro-shell")).toBeVisible();
});

test("'새 게임으로 시작'이 실제로 저장본을 지우고 인트로로 보낸다", async ({ page }) => {
  // 복구 알림을 띄운 뒤 start-fresh-after-recovery 클릭
  // → localStorage 의 trigger-prototype-v2 가 null 이고 .intro-shell 이 보일 것
});
```

---

## T1-2. 조용한 폴백을 전부 "기록되는 실패"로 바꾼다

**[증거]** D-2 표.

**[원인]** 진행이 끊기는 지점마다 `?? fallback` / `return null`이 있어 예외가 발생하지 않는다. 그래서 에러 로그에 남지 않는다.

**[지시]**
1. `src/main.jsx`에 아래 헬퍼를 추가한다(모듈 스코프, `recordAppError` 근처).
   ```js
   function reportSilentFailure(code, detail) {
     const error = new Error(`[silent:${code}] ${JSON.stringify(detail)}`);
     error.name = "SilentRouteFailure";
     recordAppError(error, {}, `silent-${code}`);
     if (import.meta.env.DEV) console.warn(error);
     return error;
   }
   ```
2. 다음 지점에서 폴백이 **실제로 발동했을 때만** `reportSilentFailure`를 호출한다. 폴백 자체는 유지한다(플레이어를 크래시시키지 않기 위해).

   | 위치 | code | detail |
   |---|---|---|
   | `main.jsx:956` `isSavedStateShapeValid` 실패 | `save-shape` | 누락/불일치 키 목록 |
   | `main.jsx:551` `repairSavedRoute` 보정 발생 | `route-repair` | `{ from, to, currentCase }` |
   | `main.jsx:1050` `resolvedNodeId` 폴백 | `unknown-node` | `{ nodeId, currentCase }` |
   | `main.jsx:1066` `nodes.start` 폴백 | `node-missing` | `{ resolvedNodeId }` |
   | `main.jsx:661` `normalizeSavedLogEntry` 반환 null | `log-entry-drop` | `{ nodeId, index }` |
   | `choose()` 진입부(신규) | `bad-next` | `{ from, choiceId, next }` |
3. `choose()` 맨 앞에 `choice.next` 유효성 검사를 추가한다.
   ```js
   const RESULT_NODE_IDS = new Set(Object.values(caseResultNodeIds));
   if (!nodes[choice.next] && !RESULT_NODE_IDS.has(choice.next)) {
     reportSilentFailure("bad-next", { from: resolvedNodeId, choiceId: choice.id, next: choice.next });
     setIsAdvancing(false);
     return;
   }
   ```
4. `window.addEventListener("error")` 핸들러 근처에서 `console.error`도 후킹해 에러 로그에 남긴다(원본 `console.error`는 계속 호출).

**[금지]**
- 폴백을 제거하고 그냥 throw 하도록 바꾸지 말 것(플레이어 경험이 더 나빠짐).
- 에러 로그 정원(`ERROR_LOG_MAX_ITEMS`)을 늘려서 해결하려 하지 말 것. T1-1을 먼저 끝내면 가짜 에러가 사라진다.

**[검증]**
- `?debug=1`로 진입 → `localStorage`에 `{"currentCase":"case03","nodeId":"start"}`처럼 어긋난 저장본을 심고 새로고침 → 디버그 에러 로그 패널에 `silent-route-repair` 항목이 보일 것.
- `npm run verify` 통과.

---

## T1-3. 누적 상태 완주 크롤러 e2e 도입 (D-11, D-12)

**[증거]** D-11, D-12. 단발 306쌍은 이미 0 failure임을 확인했으므로, 이 TASK의 초점은 **누적 상태·케이스 전환·저장 왕복**이다.

**[원인]** e2e가 항상 0번 선택지만 클릭하고, 케이스 전환은 디버그 점프로 우회한다.

**[지시]**
`tests/full-coverage.spec.js`를 새로 만든다. 세 개의 테스트를 넣는다. **(b)와 (c)가 이 TASK의 핵심이다. (a)는 회귀 방지용이다.**

**(a) 전수 (node, choice) 단발 크롤 (회귀 방지)** — 306쌍 각각에 대해 디버그 점프 → 해당 선택지 클릭 → 확정 → 다음 화면 진입까지 수행하고, `pageerror` / `console.error` / DOM에 `.error-screen`이 나타나면 실패로 기록한다. 실패는 즉시 중단하지 말고 **전부 모아서 마지막에 한 번에 리포트**한다. (현재 기준선: 0 failure. 여기서 실패가 나기 시작하면 회귀다.)

```js
import { test } from "@playwright/test";
import { nodes, nodeOrders } from "../src/gameData.js";

test("모든 (장면, 선택지) 쌍이 런타임 에러 없이 다음 화면으로 진행된다", async ({ page }) => {
  test.setTimeout(45 * 60_000);
  const failures = [];
  const errors = [];
  await page.addInitScript(() => { try { localStorage.clear(); } catch {} });
  page.on("pageerror", (e) => errors.push(`pageerror: ${e.message}`));
  page.on("console", (m) => { if (m.type() === "error") errors.push(`console: ${m.text()}`); });

  for (const caseId of ["case01","case02","case03","case04","case05","final"]) {
    for (const nodeId of nodeOrders[caseId]) {
      const scene = nodes[nodeId];
      for (let ci = 0; ci < scene.choices.length; ci += 1) {
        const choice = scene.choices[ci];
        errors.length = 0;
        try {
          await page.goto("/?debug=1");
          await page.getByTestId("debug-case-select").selectOption(caseId);
          await page.getByTestId("debug-node-select").selectOption(nodeId);
          await page.getByTestId("debug-start-node").click();
          await page.waitForSelector(".game-shell", { timeout: 8000 });
          if (choice.type === "free") {
            await page.locator(".reframe-box textarea").fill("직원과 협력사 조건을 분리하고 원본 로그를 확인한 뒤, 위험을 공개한다.");
            await page.locator(".submit-reframe").evaluate((b) => b.click());
          } else {
            const fixedIndex = scene.choices.slice(0, ci + 1).filter((c) => c.type !== "free").length - 1;
            await page.locator(".choices .choice").nth(fixedIndex).evaluate((b) => b.click());
            await page.getByTestId("commit-confirm").evaluate((b) => b.click());
          }
          await page.waitForSelector("[data-testid='decision-next']", { timeout: 8000 });
          await page.getByTestId("decision-next").evaluate((b) => b.click());
          await page.waitForSelector(".game-shell, .result-page, .ending-reveal", { timeout: 8000 });
          if (errors.length) failures.push(`${caseId}/${nodeId}/${choice.id}: ${errors.slice(0,2).join(" | ")}`);
        } catch (err) {
          failures.push(`${caseId}/${nodeId}/${choice.id}: THREW ${String(err).split("\n")[0]}`);
        }
      }
    }
  }
  if (failures.length) throw new Error(`${failures.length}건 실패\n` + failures.join("\n"));
});
```

**(b) 시드 기반 랜덤 완주 20회 ★핵심** — case01부터 final까지 6개 케이스를 연속으로, 매 장면 무작위 선택지로 완주한다.

- 케이스 전환은 **반드시 실제 "다음 사건 시작" 버튼**으로 한다. 디버그 점프 금지 — `startCase()`의 이월 효과(`getOutcomeCarryover`), 연속 챌린지(`getContinuityChallenge`), 분기 라우팅(`caseOpeningRoutes`)이 이 경로에서만 실행된다. **지금 이 경로는 사실상 미검증 상태다.**
- 매 케이스 완료 시 다음을 단언한다: `completedCases`에 해당 케이스 추가됨 / `caseResults[caseId].outcomeChoiceId`가 `caseOpeningRoutes`의 키와 일치 / 다음 케이스 첫 노드가 `caseOpeningRoutes[next][outcomeChoiceId]`와 일치.
- 완주 후 `completedCases.length === 6`, `.ending-reveal` 표시.
- 각 시드는 실패 시 재현 가능하도록 시드 값을 에러 메시지에 포함한다.

**(c) 저장 왕복 스트레스 ★핵심** — (b)와 같은 완주를 하되, **케이스마다 1회 이상 `page.reload()`** 를 끼워 넣는다. 매 reload 직후 다음을 단언한다.

```
- .game-shell 이 다시 보인다 (인트로로 튕기지 않는다)
- nodeId / currentCase / log.length 가 reload 전과 동일하다
- discoveredClues 가 줄어들지 않았다
- paused === false
- lastError 가 새로 생기지 않았다
```

추가로 `P`(저장) → 새로고침 → 이어하기, `Shift+P`(저장 후 나가기) → 이어하기, 자유입력 300자 타이핑 → 새로고침 경로를 각각 1회씩 포함한다.

**[금지]**
- (a)~(c)를 `npm run verify`의 기본 경로에 넣지 말 것. 실행이 길다(단발 306쌍만 7.7분). `npm run test:e2e:full` 별도 스크립트로 분리하고 CI에서는 nightly로 돌린다.
- 실패를 만나면 즉시 던지지 말 것. 전수 결과가 필요하다.
- 이 테스트를 통과시키려고 프로덕션 코드에 `try/catch`를 덧대지 말 것.
- 케이스 전환을 디버그 점프로 대체하지 말 것. 그러면 이 TASK의 의미가 사라진다.

**[검증]**
```bash
npm run test:e2e:full
```
실패 목록이 비어 있을 것. **비어 있지 않다면 그 목록이 곧 "에러가 나는 그 지점"의 정답지다.** 목록을 그대로 다음 TASK의 입력으로 쓴다.

---

## T1-4. 상수 단일화 (D-8)

**[지시]**
1. `src/gameData.js`에 다음을 export 한다(이미 존재하는 데이터에서 파생, 하드코딩 금지).
   ```js
   export const CASE_SEQUENCE = ["case01","case02","case03","case04","case05","final"];
   export const CASE_START_NODES = { case01:"start", case02:"c2_start", case03:"c3_start", case04:"c4_start", case05:"c5_start", final:"f_start" };
   export const CASE_RESULT_NODES = { case01:"result", case02:"case02_result", case03:"case03_result", case04:"case04_result", case05:"case05_result", final:"final_result" };
   export const RESULT_NODE_IDS = new Set(Object.values(CASE_RESULT_NODES));
   ```
2. `main.jsx`에서 다음을 **전부 삭제하고** 위 import로 대체한다.
   - `main.jsx:150` `caseSequence`
   - `main.jsx:521` `baseCaseStartNodes` (모듈)
   - `main.jsx:1051` `baseCaseStartNodes` (App 내부 재선언)
   - `main.jsx:529` `caseResultNodeIds`
   - `main.jsx:1065-1072` `isResult`의 OR 체인 → `RESULT_NODE_IDS.has(nodeId)`
   - `main.jsx:2502-2509` `nextCompletedCases`의 OR 체인 → `RESULT_NODE_IDS.has(nextNode)`
   - `main.jsx:2066-2077` `startCase()`의 if/else 사다리 → `CASE_START_NODES[caseId]`
3. `smoke-test.mjs:362`의 로컬 Set도 import로 대체한다.
4. `smoke-test.mjs`에 assert 추가: `Object.keys(CASE_START_NODES)`와 `CASE_SEQUENCE`와 `Object.keys(nodeOrders)`가 동일 집합일 것.

**[금지]**
- `main.jsx` 안에서 이 상수들을 다시 선언하지 말 것.
- 이 TASK에서 다른 리팩터링(컴포넌트 분리 등)을 함께 하지 말 것.

**[검증]** `npm run verify` 통과 + `grep -n "baseCaseStartNodes\|case02_result" src/main.jsx` 결과가 import 라인 외에 없을 것.

---

## T1-5. 진행률 계산 수정 (D-4)

**[지시]**
1. `gameData.js`에 케이스별 **실제 플레이 경로 길이**를 계산해 export 한다. 분기 브리핑 3개는 1개로 세고, 도달 불가능한 base start 노드는 제외한다.
   ```js
   export function getCaseRouteLength(caseId) { /* 시작 노드 하나를 골라 next를 따라가며 센다 */ }
   export function getNodeRouteIndex(caseId, nodeId) { /* 위 경로에서의 0-based 인덱스 */ }
   ```
2. `main.jsx:3019`의 progress를 이 함수 기반으로 교체한다.
3. `smoke-test.mjs`에 assert: 어떤 분기로 진입하든 첫 장면의 progress는 동일해야 하고, 마지막 장면은 100%여야 한다.

**[금지]** `nodeOrders`의 `unshift` 자체를 되돌리지 말 것 — 디버그 노드 선택 UI가 그 배열에 의존한다(`main.jsx:1048`).

**[검증]** `npm test` + 수동: case02를 세 분기로 각각 시작해 첫 진행률이 같은지 확인.

---

## T1-6. 소소한 확정 버그 일괄 수정 (D-5, D-6, D-7, D-9, D-10)

**[지시]** 아래 5건을 한 커밋으로 처리한다.

| # | 위치 | 수정 |
|---|---|---|
| D-5 | `gameLogic.js:508` | `const fatigueBonus = (ledger.totals.fatigue ?? 0) < 0 ? 6 : 0;` 로 분리한 뒤 `const stabilizerScore = ledger.riskDrops * 3 - ledger.riskRises + fatigueBonus;` |
| D-6 | `main.jsx:2214` | CRISIS PROTOCOL 엔트리에 `isSystemEvent: true` 추가. 추가 후 `getGameplayStats`의 `challengeClearCount`/`currentChallengeStreak`가 `entries`가 아니라 `scoredEntries`(시스템 이벤트 제외)를 쓰도록 `gameLogic.js:216-223` 수정 |
| D-7 | `main.jsx:2248` | `buildCaseSummary(nextTriggers, nextCognition, nextLog, nextResources)` 로 시그니처를 바꾸고 `choose()`에서 `finalResourcesWithTempo`를 넘김 |
| D-9 | `main.jsx:2158` | `persist({freeText})`를 400ms 디바운스. 단, `choose()` / `saveCurrentGame()` / pagehide 직전에는 즉시 flush |
| D-10 | `main.jsx:3126` | `key={line}` → `key={`${index}-${line.slice(0,12)}`}` |

**[금지]** 이 6건 외의 "겸사겸사" 수정 금지.

**[검증]** `npm run verify` + `smoke-test.mjs`에 D-5, D-6에 대한 회귀 assert를 각각 1개씩 추가.

---

## T1-7. 텍스트 자동 치환을 원고 밖으로 몰아낸다 (D-3)

**[증거]** D-3 표. 엔딩 대사 포함 12건의 조사 파괴.

**[지시]**
1. `simplifyPlayerText()`의 적용 대상을 **UI 라벨/시스템 문구로만** 제한한다. 다음 호출은 **전부 제거**한다.
   - `main.jsx:4754` `node.text` (사건 본문)
   - `main.jsx:4527` `node.title`
   - `main.jsx:4913`, `4907`, `4885` 등 `choice.label`
   - `main.jsx:3126` `renderSceneLines` 내부
   - `main.jsx:4798` `echo`
2. 대신 `gameData.js`의 원고 자체를 플레이어 언어로 **직접 다시 씁니다.** 정규식으로 덮지 않습니다.
   - "로그" → 문맥에 따라 "접속 기록" / "원본 기록" / "시스템 기록"으로 **손으로** 교체
   - "케이스" → "사건"으로 손으로 교체 (단 `케이스데스크`는 고유명사이므로 유지하거나 "사건 데스크"로 리네이밍 결정)
   - "트리거" → 게임 내 고유명사이므로 **유지**. `트리거랩`과 함께 세계관 용어로 남긴다. 대신 첫 등장 시 1회 설명 툴팁을 붙인다.
   - "분석관" → "담당자"로 바꿀지 유지할지 결정하고 **전 파일에서 일관 적용**
3. `simplifyPlayerText`에 남길 규칙은 영문 대문자 라벨(`SIGNAL WATCH`, `DECISION WINDOW`, `LIVE LEDGER` 등)만으로 축소한다. 한글→한글 치환 규칙(50~66줄)은 전부 삭제한다.
4. `smoke-test.mjs`에 회귀 방지 assert를 추가한다.
   ```js
   // 모든 노드의 text/title/memo/choice.label 에 대해
   assert.equal(simplifyPlayerText(s), s, `${id}: 원고는 런타임 치환으로 변형되면 안 된다`);
   ```

**[금지]**
- 치환 규칙에 예외 패턴을 추가해서 우회하지 말 것. 규칙 자체를 원고에서 분리하는 것이 목적이다.
- 원고 수정은 **의미를 바꾸지 않는 범위**로 한정. 문장을 새로 쓰지 말 것(그건 PHASE 4의 일).

**[검증]** 위 assert 포함 `npm test` 통과. 수동으로 `f_choice`, `f_archive`, `c2_logs`, `f_start`, `competitor`, `c3_trap` 6개 장면을 눈으로 확인.

---

# PHASE 2 — "다 잡히는" 관측 체계

PHASE 1을 끝내면 가짜 에러가 사라지고 조용한 실패가 기록되기 시작합니다. 이제 그것을 **읽을 수 있게** 만듭니다.

## T2-1. 세션 트레이스 도입

**[지시]**
플레이 중 발생하는 **모든 라우팅 전이**를 링 버퍼(최근 200개)에 기록한다. 에러가 아니라 **정상 전이도 전부** 기록한다.

```js
// 각 항목
{ t: 12345, kind: "enter" | "choose" | "case-start" | "result" | "silent" | "error",
  caseId, nodeId, choiceId, nextNodeId, logLength, resources, note }
```

- 저장 위치: `sessionStorage`의 `critical-point-trace-v1` (localStorage 아님 — 세이브를 오염시키지 않기 위해)
- 에러 발생 시 `createErrorRecoveryEntry()`가 이 트레이스 전체를 `entry.trace`로 첨부한다.
- 디버그 패널에 "트레이스 복사" 버튼을 추가한다.

**[금지]** 기존 `log` 배열에 섞지 말 것. `log`는 게임플레이 기록이고 트레이스는 진단 기록이다.

**[검증]** 에러를 인위적으로 발생시킨 뒤 진단 로그 JSON에 `trace` 배열이 200개 이하로 들어 있을 것.

## T2-2. 재현 링크

**[지시]** 에러 로그 항목마다 `?replay=<base64(seed)>` 링크를 생성한다. seed에는 `caseId`, `nodeId`, `resources`, `log`의 `choiceId` 배열만 담는다(자유입력 원문·개인정보 제외). 링크로 진입하면 그 상태를 그대로 복원한다.

**[검증]** e2e: 임의 상태에서 링크 생성 → 새 컨텍스트에서 열기 → `nodeId`/`resources` 일치.

## T2-3. 개발 모드 오버레이

**[지시]** `?debug=1`일 때 화면 우하단에 항상 표시되는 작은 배지: `caseId / nodeId / 경로 인덱스 / 자원 요약 / 최근 silent 카운트`. 클릭하면 트레이스 패널.

**[금지]** 프로덕션 번들에 포함되지 않도록 `import.meta.env.DEV` 가드를 유지할 것.

---

# PHASE 3 — UI/UX 재설계

## 3.1 지금의 문제 (정량)

`.game-board`의 **직속 자식만** 10개입니다.

```
mission-strip · pressure-cascade · suspense-console · progress-wrap ·
game-hud · live-ledger · scene-challenge · narrative-spine · scene · choice-panel
```

그 안에 다시 이런 블록들이 들어 있습니다.

```
legacy-panel · streak-meter · suspense-meter · quest-panel(3항목) · turn-brief(5칸) ·
resource-list(6칸) · status-board · lab-trace · scene-visual · speaker-card ·
scene-story · scene-beat · scene-beat-preview · trigger-tags · echo-probe ·
decision-forecast(3렌즈 + 전체 목록) · prompt-chips ·
reframe-box + reframe-signals + reframe-preview · decision-rail · decision-dock · timer-meter
```

총 약 30개 블록, 텍스트 카운터만 30개 이상. 폰트 크기 선언의 76%가 13px 이하. 미디어쿼리는 6개뿐.

**그리고 결정적으로, 게임이 정답을 알려줍니다.** `decision-forecast`(`main.jsx:4899`)가 각 선택지에 대해 **전술 등급(A/B/C) + 정확한 위험 증감 + 확정 후 압력 수치 + 최대 이득/최대 비용**을 커밋 전에 전부 보여줍니다. 여기에 "가장 안전한 관점" / "대가를 크게 쓰는 관점" 바로가기 버튼까지 있습니다. 플레이어는 판단하지 않고 **A등급을 고릅니다.**

## 3.2 목표 원칙

| 원칙 | 규칙 |
|---|---|
| 3층 정보 구조 | **1층(항상)**: 장면 텍스트 + 선택지 + 시간. **2층(요청 시)**: 자원 상세·예보·전술. **3층(결과에서만)**: 통계·랭킹·카운터팩추얼 |
| 한 화면 한 질문 | 플레이 화면에 동시에 보이는 "숫자 위젯"은 **최대 3개** |
| 정보는 비용을 지불하고 얻는다 | 예보/전술 상세는 시간이나 피로를 소비해야 열린다 (이미 있는 `echo-probe` 패턴을 확장) |
| 타입 스케일 | 5단계만: 12 / 14 / 16 / 20 / 28px. 본문 최소 16px, 모바일 최소 15px |
| 토큰 사용 | 색·간격·라운드·그림자는 전부 `var(--)`. 하드코딩 hex 신규 추가 금지 |

## T3-1. 정보 3층 구조로 재배치

**[지시]**
1. **1층(기본 화면)** 에 남길 것만 남긴다.
   - 장면 제목 + 화자 + 본문
   - 선택지 목록 (라벨 + 짧은 부제 한 줄, **수치 없음**)
   - 상단 얇은 바: `사건 N · 진행률 · 남은 시간` (이 3개가 유일한 상시 숫자)
2. **2층** 은 하나의 서랍(drawer)으로 통합한다. 토글 1개(`탭` 키 또는 "상황판" 버튼). 안에: 자원 6종, 위험 압력, 압박 원인, 장면 목표, 이월 효과, 트리거랩 관찰 기록.
   - 현재의 `narrative-spine`, `quest-panel`, `live-ledger`, `turn-brief`, `status-board`, `lab-trace`, `streak-meter`, `suspense-meter`를 **이 서랍 안으로 이동하거나 삭제**한다.
3. **3층** 은 결과 화면으로 이동한다. `decision-forecast` 전체를 플레이 화면에서 제거하고, 결과 화면의 `counterfactual-panel`에 흡수한다.

**[금지]**
- 패널을 "접힘 상태로 유지"하는 식으로 넘어가지 말 것. **DOM에서 제거하거나 서랍으로 이동**한다. 접힌 패널도 스캔 비용이다.
- 이 TASK에서 CSS 리팩터링을 같이 하지 말 것(T3-3).

**[검증]**
- e2e: `.game-board`의 **직속** `section`/`div` 개수가 **4개 이하** (현재 10개: `mission-strip`, `pressure-cascade`, `suspense-console`, 진행바, `game-hud`, `live-ledger`, `scene-challenge`, `narrative-spine`, `scene`, `choice-panel`).
- e2e: 서랍이 닫힌 상태에서 플레이 화면에 보이는 숫자 텍스트 노드가 **5개 이하**.
- e2e: `.decision-forecast`가 플레이 화면에 존재하지 않는다.
- 기존 접근성 테스트(`tests/accessibility.spec.js` 8개) 전부 통과.

## T3-2. 선택 확정 인터랙션 재설계

**[지시]**
현재는 `선택 클릭 → commit-console 표시 → 확정 버튼 → decision-reveal 모달`의 4단계입니다. 이를 다음으로 바꿉니다.

1. 선택지 카드에 **호버/포커스 시** 그 선택이 "누구에게 비용을 옮기는지"를 **문장 한 줄**로만 표시한다(수치 없음). 예: *"협력사 3곳의 지급이 뒤로 밀립니다."*
2. 확정은 **누르고 있기(press-and-hold 600ms)** 또는 두 번째 클릭. 되돌릴 수 없다는 감각을 물리적으로 만든다. (키보드는 `Enter` 두 번, 스크린리더는 기존 버튼 유지)
3. `decision-reveal`은 모달 덤프가 아니라 **장면 안에서 이어지는 비트**로 바꾼다. 3초간: 화자의 반응 1문장 → 자원 변화 1줄 → "다음" 버튼. 보너스 배지는 최대 2개까지만, 나머지는 서랍에.

**[금지]**
- 새 애니메이션 라이브러리 도입 금지. CSS transition만 사용.
- `prefers-reduced-motion` 분기를 반드시 유지할 것(`styles.css:6401`).

**[검증]** e2e에서 press-and-hold와 키보드 경로 모두 통과. `tests/accessibility.spec.js` 통과.

## T3-3. CSS 토큰화 및 축소

**[지시]**
1. `:root` 블록을 **1개로 통합**(현재 `styles.css:1`과 `:5445` 두 개)하고 토큰을 확장한다: 색 12개, 간격 6단계, 타입 5단계, 라운드 3단계, 그림자 3단계.
2. T3-1에서 제거된 패널의 CSS를 **삭제**한다. 남기지 말 것.
3. 남은 하드코딩 색을 토큰으로 치환한다. 목표: `var(--)` 사용 200회 이상, 하드코딩 hex 30종 이하.
4. 미디어쿼리를 3개 브레이크포인트(`--bp-sm: 480px`, `--bp-md: 768px`, `--bp-lg: 1024px`)로 정리한다.

**[금지]**
- CSS 프레임워크/전처리기 도입 금지.
- `!important` 신규 추가 금지(현재 4개).
- 파일 분할은 하되 **최대 4개**까지만: `tokens.css`, `base.css`, `play.css`, `report.css`.

**[검증]**
```bash
grep -c "var(--" src/*.css      # 200 이상
grep -oE "#[0-9a-fA-F]{3,8}\b" src/*.css | sort -u | wc -l   # 30 이하
wc -l src/*.css                 # 합계 4000줄 이하
npm run build
```

## T3-4. `main.jsx` 분할

**[지시]** `App()` 5,410줄을 다음으로 나눈다. **로직 변경 없이 이동만** 한다.

```
src/App.jsx              상태 + 라우팅 (목표 800줄 이하)
src/state/useGameSave.js persist / 복구 / 정규화
src/state/useDecision.js choose / preview / protocol / probe
src/screens/IntroScreen.jsx
src/screens/PlayScreen.jsx
src/screens/ResultScreen.jsx
src/screens/RankingScreen.jsx
src/components/...       DecisionReveal, RecoveryNotice, ErrorLogPanel, StatusDrawer 등
```

**[금지]**
- 이 TASK에서 동작을 바꾸지 말 것. **순수 이동 + import 정리만.** 한 줄이라도 로직을 고치면 다음 TASK로 미룬다.
- 상태를 Context/Redux로 옮기지 말 것. props와 커스텀 훅으로 충분하다.

**[검증]** `npm run verify` 및 `npm run test:e2e:full` 이 **분할 전과 동일한 결과**를 낼 것. 스냅샷 비교를 위해 분할 직전 커밋에서 전체 e2e를 한 번 돌려 결과를 저장해 둘 것.

---

# PHASE 4 — 게임성·재미

## 4.1 현재 게임의 구조적 문제

| 문제 | 근거 |
|---|---|
| 분기가 없다 | 87노드 전부 `next` 1개. 선택은 자원 숫자만 바꾼다 |
| 완전정보 | 커밋 전에 정확한 수치 + A/B/C 등급 노출 (`main.jsx:4899`) |
| 결정론적 | 같은 선택 = 항상 같은 결과. 실패의 위험이 없다 |
| 목표가 기계적 | `sceneChallenge`가 `effect.trust > 0` 같은 규칙이고, 예보 패널이 어느 선택이 그걸 만족하는지 알려준다 |
| 자유입력이 형식적 | `scoreFreeText`가 정규식 4개로 채점. 무슨 말을 써도 다음 장면은 같다 |
| 반복 플레이 유인 부족 | 두 번째 플레이에서 달라지는 것은 3갈래 도입 문단뿐 |

## T4-1. 진짜 갈림길을 만든다 (최소 개입, 최대 효과)

**[지시]**
케이스마다 **정확히 1개의 진짜 분기점**을 만든다. 총 6개.

- 각 케이스의 중반 노드(예: case01은 `competitor`)에서 선택지를 두 갈래로 나눈다.
  - 갈래 A: 기존 경로
  - 갈래 B: **새 장면 2개**(신규 집필) 후 원래 경로에 합류
- 합류 지점은 각 케이스의 `*_final` 직전으로 고정한다.
- 어느 갈래를 탔는지 `log`에 남기고, 후폭풍(`*_aftershock`) 장면의 텍스트가 그것을 **직접 인용**하게 한다.

이렇게 하면 경로 조합이 케이스당 2배, 시즌 전체 2^6 = 64가지가 됩니다. 신규 집필은 12장면입니다.

**[금지]**
- 분기를 템플릿으로 자동 생성하지 말 것. `addConnectiveScene`/`addReactionScene` 패턴을 재사용하지 말 것. **12장면 전부 개별 집필.**
- 분기 수를 늘리려 하지 말 것. 케이스당 1개. 콘텐츠 품질이 개수보다 중요하다.

**[검증]** `smoke-test.mjs`에 assert: 각 케이스에 `next`가 2개 이상인 노드가 정확히 1개 존재. `full-coverage` e2e가 두 갈래 모두 통과.

## T4-2. 완전정보를 제거하고 "정보를 사는" 구조로

**[지시]**
1. 기본 화면에서 수치 예보를 제거한다(T3-1에서 이미 수행).
2. 서랍의 예보는 **방향과 폭만** 보여준다: `위험 ↑ 크게` / `믿음 ↓ 조금` / `??` (불확실).
3. 불확실성을 도입한다. 장면마다 "확보한 근거 수"(memo 열람, 에코 프로브, 자유입력 신호)에 따라 예보의 정확도가 달라진다.
   - 근거 0개: 모든 선택지가 `??`
   - 근거 1~2개: 방향만 (↑/↓)
   - 근거 3개 이상: 방향 + 폭
4. 실제 결과에 **±15% 변동**을 준다. 단, 시드는 `sessionId + nodeId + choiceId`로 결정론적으로 만든다(리플레이/테스트 재현성 유지).
5. `tacticalRead`의 A/B/C 등급 노출을 제거한다. 등급은 결과 화면 회고에서만 보여준다.

**[금지]**
- `Math.random()` 직접 사용 금지. 반드시 시드 기반 PRNG를 `gameLogic.js`에 순수 함수로 추가하고 테스트할 것.
- 난이도 조절을 자원 수치 튜닝으로만 하지 말 것. 정보 접근성이 진짜 난이도 레버다.

**[검증]** `smoke-test.mjs`: 같은 시드 입력에 같은 출력. `getGameplayStats`의 회귀 assert 유지.

## T4-3. 자유입력을 진짜 분기로 승격

**[지시]**
현재 `scoreFreeText`는 자원만 바꿉니다. 다음을 추가합니다.
- 신호 4종(`stakeholder`/`tradeoff`/`info`/`risk`) 중 **3개 이상**을 만족하면 그 장면의 후속 장면에서 **에코가 플레이어의 문장을 인용**한다.
- 인용문은 `log[].freeText`에서 가져오되, 개인정보 감지(`detectPrivacySignals`)가 걸린 경우 인용하지 않는다.
- 케이스당 자유입력 성공 2회 이상이면 후폭풍 장면에 **전용 선택지 1개**가 추가로 열린다.

**[금지]** LLM 호출 등 외부 의존 추가 금지. 순수 규칙 기반으로 구현한다.

**[검증]** e2e: 조건 충족 입력 후 다음 장면 텍스트에 입력 문구가 포함되는지 확인.

## T4-4. 자동 생성 36장면의 품질 개선

**[지시]**
`connective` 18개 + `reaction` 18개의 선택지 효과 템플릿(`gameData.js:1685`, `:1755`)을 **장면별 개별 값**으로 교체한다. 최소한:
- 각 장면의 3개 선택지가 **서로 다른 자원 축**을 건드릴 것
- 같은 케이스 안에서 동일한 effect 객체가 두 번 나오지 않을 것
- `choiceVoiceLines`와 `echoReplies`에 이 36장면 × 3선택지 = 108개 항목을 채울 것 (현재 147개 선택지가 기본 문구로 폴백 중)

**[금지]** 다시 템플릿 함수로 생성하지 말 것. 데이터를 명시적으로 나열한다. 파일이 길어지는 것은 허용한다.

**[검증]** `smoke-test.mjs`:
- 케이스 내 effect 중복 0
- `choiceVoiceLines`/`echoReplies` 폴백 발생 선택지 수가 **20개 이하**(현재 147개)

## T4-5. 숨은 관찰 점수(플레이어에게 보이지 않는 장부)

**[지시]**
트리거랩이 플레이어를 관찰하는 4개 지표를 백그라운드로 누적한다. **플레이 중 절대 노출하지 않는다.**

| 지표 | 누적 조건 |
|---|---|
| `compliance` | 예보/추천을 따라간 횟수, 응답 2초 이하 |
| `defiance` | 자유입력, 예상 위험이 높은 선택 |
| `opacity` | 공개를 미룬 선택, 침묵 선택 |
| `sacrifice` | humanCost가 오르는 선택을 감수한 횟수 |

이 값은 **PHASE 5의 반전 재료**입니다. 엔딩에서 처음 공개됩니다.

**[검증]** `smoke-test.mjs`에서 4지표가 결정론적으로 계산되는지. 플레이 화면 DOM에 이 값이 노출되지 않는지 e2e로 확인.

---

# PHASE 5 — 엔딩의 반전과 여운

## 5.1 현재 엔딩의 문제

- 엔딩 텍스트가 **분석 리포트 5개 섹션 뒤에** 나옵니다(`main.jsx:4401`, `result-page` 안). 클라이맥스가 통계에 파묻힙니다.
- 반전이 없습니다. `endingProfile`(`main.jsx:2913`)은 마지막 선택 3개에 대응하는 고정 텍스트 3개입니다.
- 엔딩 후 화면이 없습니다. "다시 플레이 / 랭킹" 버튼으로 끝납니다.
- `{clueCount}/6`은 D-1 때문에 항상 0/6입니다.

## T5-1. 엔딩을 리포트에서 분리한다

**[지시]**
`final` 케이스 완료 시 **전용 화면**으로 전환한다. 순서:

```
1. 암전 + 마지막 선택 인용 (플레이어가 실제로 고른 문장)
2. 반전 시퀀스 (T5-2)
3. 여운 시퀀스 (T5-3)
4. ── 여기서 처음으로 ── "기록 열람" 버튼 → 기존 분석 리포트
```

**[금지]** 리포트를 지우지 말 것. **순서만** 바꾼다.

## T5-2. 반전 설계 — 3안 중 택1 (또는 조합)

이 게임의 전제("트리거랩은 사고가 깊어지는 조건을 연구한다")를 뒤집는 반전 후보입니다. 셋 다 **이미 존재하는 데이터로 구현 가능**합니다.

### 반전 A — "당신은 첫 번째 참가자가 아니었다" (가장 저비용, 기존 복선 활용)
`f_witness` 장면("보관소 안에는 당신보다 먼저 실험을 통과한 사람의 기록이 있습니다")이 이미 복선입니다.
- 엔딩에서 그 "이전 참가자"의 선택 로그가 공개되는데, **플레이어 자신의 케이스 01 선택과 정확히 일치**합니다.
- 즉 플레이어는 이 실험을 이미 한 번 통과했고, 기록만 지워진 상태였습니다.
- 구현: `caseResults.case01.outcomeChoiceId`를 "이전 참가자의 기록"으로 그대로 표시.

### 반전 B — "에코는 관찰자가 아니라 이전 참가자다"
- 에코의 대사(`echoReplies`)는 항상 "그 선택의 비용을 다시 계산하라"고 말합니다. 그건 조언이 아니라 **후회**였습니다.
- 엔딩에서 에코의 프로필이 열리는데, `primaryTrigger`가 플레이어와 동일합니다.
- 구현: `getDecisionFingerprint`의 결과를 에코의 프로필로 재사용.

### 반전 C — "실험의 결과물은 보고서가 아니라 다음 사건이다" (T4-5 활용)
- 숨겨둔 4지표(`compliance`/`defiance`/`opacity`/`sacrifice`)가 공개되며, **그 값이 이번 시즌에서 플레이어가 만난 장면들의 난이도·선택지 구성을 결정했다**고 밝혀집니다.
- 실제로 그렇게 만듭니다: 다음 플레이에서 이 4지표가 `localStorage`에 남아 케이스 02~05의 선택지 라벨/순서를 실제로 바꿉니다.
- 가장 강력하지만 구현 비용도 가장 큽니다.

**권장:** A + C 조합. A는 즉시 감정적 임팩트, C는 재플레이 동기.

**[지시]** 위 중 하나를 선택해 구현하고, 반전 공개는 **3비트**로 나눈다(한 번에 다 보여주지 말 것): 위화감 → 증거 → 확인.

**[금지]** 반전을 텍스트 한 덩어리로 출력하지 말 것. 플레이어가 "다음" 을 눌러 진행하게 한다.

## T5-3. 여운 설계

**[지시]** 반전 직후, 다음 3개를 순서대로 배치한다.

1. **조용한 한 컷** — 대사도 숫자도 없는 화면. 플레이어가 실제로 남긴 문장 중 하나(자유입력 또는 마지막 선택)를 화면 가운데에 한 줄로. 8초간 다른 버튼 없음.
2. **다음 참가자에게 남기는 한 문장** — 입력창 1개. 여기 쓴 문장이 `localStorage`에 저장되고, **다음 플레이의 인트로에서 "이전 참가자가 남긴 말"로 등장**한다. (원격 저장이 켜져 있으면 다른 플레이어의 문장이 표시될 수 있게 확장 가능)
3. **닫히지 않은 것** — 이번 플레이에서 발견하지 못한 단서 수, 밟지 않은 분기 수를 **내용 없이 개수만** 보여준다. `4개의 기록이 아직 열리지 않았습니다.`

**[금지]**
- 여운 화면에 점수·랭크·통계를 넣지 말 것.
- 공유 버튼을 여기에 넣지 말 것. 리포트 화면으로 미룬다.

**[검증]**
- e2e: `final` 완주 후 `.ending-sequence`가 리포트보다 **먼저** 나타난다.
- e2e: 남긴 문장이 새 세션 인트로에 표시된다.
- 접근성: 8초 정지 화면에 스킵 경로가 있고 `prefers-reduced-motion`을 존중한다.

---

# 6. Codex 운용 규칙 (이 저장소 특성 반영)

이 저장소의 커밋 이력에서 관찰된 패턴과 그에 대한 대응입니다.

| 관찰된 성향 | 근거 | 지시에 반드시 포함할 문구 |
|---|---|---|
| 근본 원인 대신 방어 레이어를 추가한다 | 정규화 함수 12개, 복구 슬롯, 에러 바운더리가 있는데도 D-1이 살아 있음 | "새 정규화/폴백/복구 함수를 추가하지 말 것. 기존 것을 불필요하게 만드는 방향으로 고칠 것." |
| 파일을 계속 확장한다 | `main.jsx` 5,410줄, `styles.css` 6,713줄 | "이 TASK에서 파일 총 줄 수가 늘어나면 안 된다. 늘어야 한다면 이유를 먼저 설명할 것." |
| 상수를 재선언한다 | `baseCaseStartNodes` 2회, 결과 노드 ID 4곳 | "상수는 반드시 기존 export를 import 할 것. 새로 선언하면 실패로 간주한다." |
| 테스트하기 쉬운 곳만 테스트한다 | 순수 함수 153 assert vs 화면 1경로 | "이 수정의 검증은 반드시 e2e로 할 것. 순수 함수 assert만으로는 완료가 아니다." |
| 콘텐츠를 템플릿으로 양산한다 | 자동 생성 36장면, 동일 effect 3종 | "템플릿 생성 함수를 쓰지 말고 데이터를 명시적으로 나열할 것." |
| 요청 범위를 넘어 "겸사겸사" 고친다 | 커밋 137개가 대부분 소규모 단일 개선 | "이 TASK의 [지시] 목록 외의 변경은 전부 되돌릴 것." |
| 조용한 폴백을 선호한다 | `?? nodes.start`, `return null` 다수 | "폴백을 추가할 때는 반드시 `reportSilentFailure`를 함께 호출할 것." |

### 프롬프트 템플릿

```
아래 TASK 하나만 수행해라. TASK에 없는 변경은 하지 마라.

<TASK 전문 붙여넣기>

작업 전에:
- 수정할 파일과 줄 번호를 먼저 나열해라.
- [금지] 항목을 어기게 되는 부분이 있으면 코드를 쓰기 전에 말해라.

작업 후에:
- [검증]의 명령을 실제로 실행하고 출력을 붙여라.
- 변경된 파일의 줄 수 증감을 보고해라.
```

---

# 7. 회귀 방지 테스트 매트릭스

| 계층 | 도구 | 현재 | 목표 |
|---|---|---|---|
| 그래프 정합성 | `smoke-test.mjs` | 306쌍 전수 ✅ | 유지 + 분기 노드 assert 추가 |
| 순수 로직 | `smoke-test.mjs` | 153 assert | 시드 PRNG·관찰 지표 assert 추가 |
| **원고 무결성** | `smoke-test.mjs` | ❌ | `simplifyPlayerText(원고) === 원고` (T1-7) |
| **저장 스키마** | `smoke-test.mjs` | ❌ | `SAVE_STATE_KEYS` ↔ `isSavedStateShapeValid` 일치 (T1-1) |
| **전 경로 화면** | `full-coverage.spec.js` | ❌ (1경로) | 306쌍 전수 + 랜덤 완주 20회 (T1-3) |
| 저장/복구 | `season-flow.spec.js` | 충실 ✅ | 유지 |
| 접근성 | `accessibility.spec.js` | 7개 화면 | UI 개편 후 재실행 필수 |
| 정보 밀도 | 신규 | ❌ | 플레이 화면 section ≤ 6, 상시 숫자 ≤ 5 (T3-1) |
| CSS 예산 | 신규 | ❌ | `var(--)` ≥ 200, hex ≤ 30, CSS ≤ 4,000줄 (T3-3) |

`package.json`에 추가할 스크립트:

```json
"test:e2e:full": "node scripts/run-e2e.mjs --full",
"verify": "npm test && npm run build && npm run test:e2e",
"verify:full": "npm run verify && npm run test:e2e:full"
```

CI(`.github/workflows/verify.yml`)는 PR에 `verify`, nightly에 `verify:full`.

---

# 8. 실행 순서

```
PHASE 1  (버그 근절)          T1-1 → T1-1B → T1-2 → T1-3 → T1-4 → T1-5 → T1-6 → T1-7
   ↓  게이트: T1-3의 전수 크롤 실패 0건 + 에러 로그에 save-integrity 0건
PHASE 2  (관측)               T2-1 → T2-2 → T2-3
   ↓  게이트: 임의 에러 1건을 트레이스만 보고 원인 특정 가능
PHASE 3  (UI/UX)              T3-1 → T3-2 → T3-3 → T3-4
   ↓  게이트: 정보 밀도/CSS 예산 통과 + 접근성 전 통과
PHASE 4  (게임성)             T4-1 → T4-4 → T4-2 → T4-3 → T4-5
   ↓  게이트: 시즌 경로 조합 64가지, 폴백 선택지 20개 이하
PHASE 5  (엔딩)               T5-1 → T5-2 → T5-3
```

### 즉시 착수 3가지

1. **`debug.log`를 더 이상 보지 마십시오.** 앱 로그가 아닙니다. 대신 게임 안 `?debug=1` → 에러 로그 패널 → "진단 로그" 내보내기가 진짜 데이터입니다.
2. **T1-1 + T1-1B를 가장 먼저 하십시오.** 둘이 합쳐져 "복구 배너가 뜨는데 어떤 버튼도 소용없는" 상태를 만들고 있습니다. 각각 몇 줄짜리 수정입니다.
3. **T1-3을 그다음에 하십시오.** 누적 상태 완주 크롤러의 실패 목록이 곧 "에러가 나는 그 지점"의 정답지입니다. 지금까지 그 목록을 만든 적이 없습니다.

---

## 부록 A — 이 문서를 만들며 실제로 실행한 검증

| 검증 | 결과 |
|---|---|
| 노드 그래프 정적 분석 (dangling / 도달성 / 고아 / 케이스 누수) | 전부 정상, 0건 |
| 노드 분기도 (`next` 개수) | 87/87 노드가 단일 `next` — 실질 분기 0 |
| `(node, choice)` 쌍 수 | 306 (자유입력 44 포함) |
| **306쌍 전수 화면 크롤 (Playwright, chromium, 7.7분)** | **0 failure** — 단발 전이는 무결. 따라서 버그는 누적 상태·케이스 전환·저장 왕복에 있음 (D-12) |
| 실제 인트로 시작 → 6케이스 연속 완주 (케이스 전환 버튼 사용, 케이스마다 새로고침 1회) | **완주 성공** (`final / final_result` 도달, 랭크 C, 버스트 29). 케이스 전환·이월 경로 자체는 정상. 단, 완주 시점 화면에 D-1의 가짜 "복구됨" 배너가 떠 있었음 |
| 저장본 삭제 후 페이지 이동 | **저장본 부활 확인** (D-13) |
| 복구 알림의 "새 게임으로 시작" 클릭 | **저장본 그대로 남고 같은 장면 유지** (D-13) |
| `choiceVoiceLines`/`echoReplies` 폴백 | 각 147건 |
| `simplifyPlayerText` 원고 변형 | 본문 10노드, 선택지 14건, 조사 파괴 12건 |
| `persist()` → 새로고침 저장본 손상 | Playwright로 재현 완료 (D-1 로그) |
| `debug.log` 내용 분석 | 215줄 전부 Chromium GPU/오디오, 앱 항목 0 |
| CSS 계측 | 6,713줄 / 셀렉터 1,149 / `var()` 47 / hex 127종 / 미디어쿼리 6 |
| 플레이 화면 블록 수 | 약 30개 |

## 부록 B — D-1과 D-13을 직접 확인하는 30초 절차

개발 서버를 띄운 뒤 브라우저에서:

1. `http://127.0.0.1:5173/?debug=1` 접속 → 아무 케이스나 시작
2. DevTools 콘솔에서 단서를 하나 심습니다.
   ```js
   const s = JSON.parse(localStorage.getItem("trigger-prototype-v2"));
   s.discoveredClues = [{ id: "test", title: "테스트 단서", text: "x" }];
   localStorage.setItem("trigger-prototype-v2", JSON.stringify(s));
   ```
3. 자유입력 칸에 아무 글자나 한 개 입력합니다. (또는 탭을 다른 곳으로 옮겼다가 돌아옵니다)
4. 콘솔에서 확인:
   ```js
   Object.hasOwn(JSON.parse(localStorage.getItem("trigger-prototype-v2")), "discoveredClues")
   // → false   ← 키가 통째로 사라졌습니다
   ```
5. 새로고침한 뒤 다시 확인:
   ```js
   const s2 = JSON.parse(localStorage.getItem("trigger-prototype-v2"));
   [s2.discoveredClues.length, s2.paused, s2.lastError?.source]
   // → [0, true, "save-integrity"]   ← 단서 소실 + 강제 일시정지 + 가짜 에러
   ```

화면에도 복구 알림이 뜹니다. **이 상태가 매 새로고침마다 반복됩니다.**

이어서 D-13은 이렇게 확인합니다.

6. 복구 알림의 **[새 게임으로 시작]** 을 누릅니다.
7. 화면이 인트로로 가지 않고 같은 장면 그대로입니다. 콘솔에서:
   ```js
   JSON.parse(localStorage.getItem("trigger-prototype-v2")).nodeId
   // → 지우기 전과 같은 노드 ID   ← 저장본이 리로드 직전에 되살아났습니다
   ```

---

## 부록 C — 참고 파일·줄 번호 색인

```
저장/복구
  main.jsx:953   saved useMemo (로드 진입점)
  main.jsx:1838  persist()                      ← D-1
  main.jsx:750   normalizeSavedNestedState()    ← 가짜 lastError 주입 783-791
  appConfig.js:76 isSavedStateShapeValid()      ← discoveredClues 요구
  appConfig.js:3  ERROR_LOG_MAX_ITEMS = 20
  main.jsx:1762  pagehide 핸들러                ← D-13 (저장본 부활)
  main.jsx:1955  startFreshAfterRecovery()      ← D-13 (지운 뒤 reload)
  main.jsx:3235  renderRecoveryNotice()         ← 가짜 배너가 뜨는 곳
  main.jsx:2641  reset()                        ← 정상 (reload 안 함)
라우팅
  main.jsx:1050  resolvedNodeId 폴백
  main.jsx:1066  nodes.start 폴백
  main.jsx:2065  startCase()
  main.jsx:2386  choose()                       ← next 검증 없음 2500
  main.jsx:2763  startAtNode() (디버그 점프)
  gameData.js:1633 aftermathRoutes
  gameData.js:1685 addConnectiveScene (템플릿)
  gameData.js:1755 addReactionScene (템플릿)
  gameData.js:1835 nodeOrders unshift           ← D-4
화면
  main.jsx:3019  progress 계산                  ← D-4
  main.jsx:3159  renderDecisionReveal()
  main.jsx:3896  결과 화면 시작
  main.jsx:4401  엔딩 (리포트 뒤)               ← T5-1
  main.jsx:4432  플레이 화면 시작
  main.jsx:4899  decision-forecast (완전정보)   ← T4-2
로직
  gameLogic.js:508 stabilizerScore 우선순위     ← D-5
  gameLogic.js:216 challengeClearCount/streak   ← D-6
  playerLanguage.js:1 치환 규칙 67개            ← D-3
테스트
  smoke-test.mjs:360 그래프 전수 검증
  season-flow.spec.js:5 chooseFirstFixedChoice  ← D-11
```
## 상태 업데이트 (2026-08-29)

현재 형상 기준 완료율은 약 90%입니다. T1 저장·복구, T2 관측, T3 UI 구조화, T4-1/T4-2/T4-4/T4-5 게임성, T5 엔딩은 구현 및 주요 회귀 검증을 완료했습니다.

이번 업데이트에서 T4-3의 남은 핵심인 자유입력 실제 분기를 반영했습니다. 조건 기반 자유입력이 성공하면 해당 케이스의 집필 분기 장면으로 이동하고 `freeTextBranchId`를 플레이 로그에 기록합니다. 개인정보 감지에 걸린 입력은 분기 승격하지 않습니다.

남은 검증 및 운영 과제:

- T1-3 랜덤 시즌 20회 전체를 한 번의 검증 결과로 재실행하고 결과를 보관합니다.
- Supabase anon 읽기/쓰기 정책은 공개 플레이테스트 전용입니다. 운영 랭킹에는 서버 검증과 rate limit이 필요합니다.
- `simplifyPlayerText` 호출부를 원고 렌더링과 UI 라벨 렌더링으로 완전히 분리합니다. 현재는 한글 원고 보존 가드로 동작상 문제를 막은 상태입니다.

문서 내 과거 `main.jsx`/`styles.css` 줄 번호와 기존 미완료 표기는 현재 `AppContent.jsx`/`app.css` 구조 및 이 상태 업데이트를 기준으로 해석합니다.

---

## 상태 업데이트 (2026-08-29, 후속 개선 반영)

이전 상태 업데이트에서 남겨둔 검증 및 운영 보강 항목을 반영했습니다.

- T1-3 full-coverage 하네스의 상태 전환 race를 수정했습니다. decision reveal이 닫힌 뒤 다음 선택을 수행하도록 대기하며, 306개 장면·선택 조합과 랜덤 시즌 20회, reload stress를 검증합니다.
- `npm run test:e2e:full` 최종 결과: `22 passed` (Chromium, 단일 워커).
- 원격 랭킹은 원본 `playtest_sessions` 테이블을 anon에게 직접 노출하지 않고, 완주 시즌의 공개 필드만 제공하는 `public_rankings` 뷰를 사용합니다. `decision_log`는 anon 읽기 대상에서 제외했습니다.
- GitHub Actions에 `Full Coverage` 예약/수동 워크플로를 추가했습니다. 기본 `Verify`와 분리해 장시간 전체 경로 검증을 운영합니다.
- GitHub Actions checkout/setup-node는 Node 24 런타임을 사용하는 v5로 갱신했습니다.
- Supabase anon insert에도 DB 트리거 기반 payload 검증과 actor별 시간당 요청 제한을 추가했습니다. 완주 랭킹 점수와 공개 가능한 case만 서버에서 허용합니다.
- `simplifyPlayerText` 호출부를 authored copy와 UI 라벨로 분리했습니다. 장면 서술·대사·선택지·메모는 원문을 직접 렌더링하고, 단계명·상태명·시스템 라벨만 UI 변환기를 사용합니다.
- full-coverage의 독립적인 랜덤 시즌·reload stress 테스트를 4개 워커로 병렬화했습니다. 306개 전수 크롤은 단일 테스트로 유지하며, 병렬 전체 실행 결과는 `22 passed`, `failedTests: []`입니다.

현재 코드 기준으로 기능·회귀 검증 미완료 항목은 없습니다. Supabase SQL Editor에 최신 `supabase/schema.sql`을 적용하는 배포 단계만 운영 환경에서 수행하면 됩니다.

---
