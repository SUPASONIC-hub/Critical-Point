# 2026-08-29

## 상태 업데이트 (2026-08-29)

현재 형상 기준 완료율은 약 90%입니다. T1 저장·복구, T2 관측, T3 UI 구조화, T4-1/T4-2/T4-4/T4-5 게임성, T5 엔딩은 구현 및 주요 회귀 검증을 완료했습니다.

이번 업데이트에서 T4-3의 남은 핵심인 자유입력 실제 분기를 반영했습니다. 조건 기반 자유입력이 성공하면 해당 케이스의 집필 분기 장면으로 이동하고 `freeTextBranchId`를 플레이 로그에 기록합니다. 개인정보 감지에 걸린 입력은 분기 승격하지 않습니다.

남은 검증 및 운영 과제:

- T1-3 랜덤 시즌 20회 전체를 한 번의 검증 결과로 재실행하고 결과를 보관합니다.
- Supabase anon 읽기/쓰기 정책은 공개 플레이테스트 전용입니다. 운영 랭킹에는 서버 검증과 rate limit이 필요합니다.
- `simplifyPlayerText` 호출부를 원고 렌더링과 UI 라벨 렌더링으로 완전히 분리합니다. 현재는 한글 원고 보존 가드로 동작상 문제를 막은 상태입니다.

문서 내 과거 `main.jsx`/`styles.css` 줄 번호와 기존 미완료 표기는 현재 `AppContent.jsx`/`app.css` 구조 및 이 상태 업데이트를 기준으로 해석합니다.

---

## Status Update (2026-08-29, structural follow-up)

- The largest remaining structural risk, save and recovery lifecycle ownership inside `AppContent.jsx`, is now exposed through `src/state/useAppPersistence.js`.
- Existing save schema, recovery slots, error log repair, and browser pause/resume behavior were verified without changing the public payload contract.
- Verification baseline: `npm test`, `npm run build`, and the accessibility/season E2E set (`88 passed`).
- No additional feature item is currently unverified in the repository. Applying `supabase/schema.sql` remains an operations deployment step, not a code change.
- T4-2 follow-up: decision previews now expose a directional pressure range until enough evidence is collected; precise risk deltas are revealed only after the evidence threshold is reached.
- CI follow-up: final ending reduced-motion initialization now makes the continuation control available on the first render, avoiding the mobile timing race seen in `npm run verify`.
- T4-2 UI follow-up: the tactical panel now renders the evidence-sensitive forecast comparison, making the uncertainty mechanic actionable instead of leaving it as hidden state.

## 상태 업데이트 (2026-08-29, 후속 개선 반영)

이전 상태 업데이트에서 남겨둔 검증 및 운영 보강 항목을 반영했습니다.

- T1-3 full-coverage 하네스의 상태 전환 race를 수정했습니다. decision reveal이 닫힌 뒤 다음 선택을 수행하도록 대기하며, 306개 장면·선택 조합과 랜덤 시즌 20회, reload stress를 검증합니다.
- `npm run test:e2e:full` 최종 결과: `22 passed` (Chromium, 단일 워커).
- 원격 랭킹은 원본 `playtest_sessions` 테이블을 anon에게 직접 노출하지 않고, 완주 시즌의 공개 필드만 제공하는 `public_rankings` 뷰를 사용합니다. `decision_log`는 anon 읽기 대상에서 제외했습니다.
- GitHub Actions에 `Full Coverage` 예약/수동 워크플로를 추가했습니다. 기본 `Verify`와 분리해 장시간 전체 경로 검증을 운영합니다.
- GitHub Actions checkout/setup-node는 Node 24 런타임을 사용하는 v5로 갱신했습니다.
- Supabase anon insert에도 DB 트리거 기반 payload 검증과 actor별 시간당 요청 제한을 추가했습니다. 완주 랭킹 점수와 공개 가능한 case만 서버에서 허용합니다.
- `simplifyPlayerText` 호출부를 authored copy와 UI 라벨로 분리했습니다. 장면 서술·대사·선택지·메모는 원문을 직접 렌더링하고, 단계명·상태명·시스템 라벨만 UI 변환기를 사용합니다.
- full-coverage의 독립적인 랜덤 시즌·reload stress 테스트를 4개 워커로 병렬화했습니다. 306개 전수 크롤은 단일 테스트로 유지하며, 병렬 전체 실행 결과는 `22 passed`, `failedTests: []`입니다.

현재 코드 기준으로 기능·회귀 검증 미완료 항목은 없습니다. Supabase SQL Editor에 최신 `supabase/schema.sql`을 적용하는 배포 단계만 운영 환경에서 수행하면 됩니다.

---
