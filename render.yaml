services:
  - type: web
    runtime: static
    name: critical-point
    buildCommand: npm ci --include=dev && npm run verify:static && npm run build
    staticPublishPath: ./dist
    autoDeploy: true
